{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  customDatasources: {
    "grafana-dashboards": {
      defaultRegistryUrlTemplate: "https://grafana.com/api/dashboards/{{packageName}}",
      format: "json",
      transformTemplates: [
        '{"releases":[{"version": $string(revision)}], "sourceDirectory": name}',
      ],
    },
  },
  customManagers: [
    {
      customType: "regex",
      description: "Process custom images and version fields",
      fileMatch: ["(^|/).+\\.yaml$"],
      matchStrings: [
        // image: <registry>/<path>:vX.Y.Z   (e.g. 10-general.yaml)
        "#\\s*renovate:\\s*datasource=(?<datasource>[^\\s]+)\\s+depName=(?<depName>[^\\s]+)(?:\\s+packageName=(?<packageName>[^\\s]+))?\\s+image:\\s*(?<depPart>.*?:)(?<currentValue>[v\\d\\.]+)",
        // version: vX.Y.Z                   (e.g. system-upgrade-controller plans)
        "#\\s*renovate:\\s*datasource=(?<datasource>[^\\s]+)\\s+depName=(?<depName>[^\\s]+)(?:\\s+packageName=(?<packageName>[^\\s]+))?\\s+[a-z_]*version:\\s*(?<currentValue>[v\\d\\.]+)",
      ],
      datasourceTemplate: "{{{datasource}}}",
      depNameTemplate: "{{{depName}}}",
    },
    {
      customType: "regex",
      description: "Process Helmfile oci charts",
      fileMatch: ["(^|/)helmfile\\.yaml$"],
      matchStrings: [
        "chart: oci://(?<depName>[^\\s]+?)\\s+version: (?<currentValue>[^\\s]+)",
      ],
      datasourceTemplate: "docker",
      versioningTemplate: "helm",
    },
    {
      customType: "regex",
      description: "Update Grafana Dashboard IDs",
      managerFilePatterns: ["(^|/)vendir\\.yml$"],
      matchStrings: [
        "dashboards\\/(?<depName>\\d+)\\/revisions\\/(?<currentValue>\\d+)\\/download",
      ],
      datasourceTemplate: "custom.grafana-dashboards",
      versioningTemplate: "regex:^(?<major>\\d+)$",
      autoReplaceStringTemplate: "dashboards/{{depName}}/revisions/{{newValue}}/download",
    },
  ],
  packageRules: [
    {
      matchDatasources: ["custom.grafana-dashboards"],
      addLabels: ["renovate/grafana-dashboard"],
      semanticCommitScope: "grafana-dashboards",
      semanticCommitType: "chore",
      commitMessageTopic: "dashboard {{sourceDirectory}}",
      commitMessageExtra: "( {{currentVersion}} → {{newVersion}} )",
    },
    {
      description: "Vendir Group",
      groupName: "Vendir",
      matchManagers: ["vendir"],
      matchDatasources: ["git-refs", "git-tags", "github-tags"],
      group: {
        commitMessageTopic: "{{{groupName}}} group",
      },
    },
    {
      matchDatasources: ["python-version"],
      enabled: false,
    },
  ],
  lockFileMaintenance: {
    enabled: true,
    automerge: true,
    schedule: ["before 4am on Monday"],
  },
}
