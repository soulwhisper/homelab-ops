---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: volsync
  namespace: storage-system
  # server path: /{shared-path}/volsync
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  storageClassName: nfs-shared
