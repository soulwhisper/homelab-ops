---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cert-manager
spec:
  folderRef: default
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-cert-manager
    key: cert-manager.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cloudflared
spec:
  folderRef: default
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-cloudflared
    key: cloudflared.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: external-dns
spec:
  folderRef: default
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-external-dns
    key: external-dns.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: node-exporter-full
spec:
  folderRef: default
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-node-exporter-full
    key: node-exporter-full.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: smartctl-exporter
spec:
  folderRef: default
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-smartctl-exporter
    key: smartctl-exporter.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: grafana-operator
spec:
  folderRef: default
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: grafana-operator-dashboard
    key: grafana-operator.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: synology
spec:
  folderRef: infrastructure
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-synology
    key: synology.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: openwrt
spec:
  folderRef: infrastructure
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-openwrt
    key: openwrt.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: blackbox-exporter
spec:
  folderRef: devices
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_SIGNCL-PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-blackbox-exporter
    key: blackbox-exporter.json
