---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # app
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  # : due to issue:https://github.com/VictoriaMetrics/helm-charts/issues/2420
  # : we use the original manifests instead of helm chart
  # - ./patch.yaml
