---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/helm.toolkit.fluxcd.io/helmrelease_v2.json
# yamllint disable rule:quoted-strings
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name victoria-metrics-cluster
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: victoria-metrics-k8s-stack

  values:
    fullnameOverride: *name
    global:
      image:
        registry: quay.io
    victoria-metrics-operator:
      enabled: false
    defaultDashboards:
      enabled: false
    defaultRules:
      rules:
        groups:
          etcd:
            create: false
          kubernetesSystemControllerManager:
            create: false
          kubernetesSystemScheduler:
            create: false

    # : vmsingle as prometheus
    # : http://vmsingle-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8428
    vmsingle:
      spec:
        retentionPeriod: "30d"
        storage:
          storageClassName: ceph-block
          resources:
            requests:
              storage: 100Gi
        # cap series intake to protect Ceph from runaway cardinality
        extraArgs:
          storage.maxHourlySeries: "200000"
          storage.maxDailySeries: "500000"

    # : vmalert
    # : http://vmalert-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8080
    vmalert:
      spec:
        datasource:
          url: "http://vmauth-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8427"

    # : vmagent
    # : http://vmagent-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8429
    vmagent:
      spec:
        extraArgs:
          promscrape.dropOriginalLabels: "true"

    # : vmauth
    # : http://vmauth-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8427
    # processing vlogs and vmetrics alerts at same altertmanager instance
    vmauth:
      enabled: true
      spec:
        extraArgs:
          requestBufferSize: "65536"
        unauthorizedUserAccessSpec:
          url_map:
            - src_paths:
                - "/api/v1/query.*"
                - "/api/v1/label/.*"
              url_prefix: "http://vmsingle-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8428"
            - src_paths:
                - "/select/logsql/.*"
              url_prefix: "http://victoria-logs-server.monitoring-system.svc.cluster.local:9428"

    # : alertmanager
    # : http://vmalertmanager-victoria-metrics-cluster.monitoring-system.svc.cluster.local:9093
    alertmanager:
      useManagedConfig: true
      config:
        route:
          group_by: ["alertname", "job"]
          group_interval: 10m
          group_wait: 1m
          receiver: "default"
          repeat_interval: 12h
          routes:
            - receiver: "blackhole"
              matchers:
                - alertname =~ "InfoInhibitor|Watchdog"
            - receiver: "default"
              matchers:
                - severity =~ "warning|critical"
        inhibitRules:
          - equal: ["alertname", "namespace"]
            sourceMatch:
              - severity = "critical"
            targetMatch:
              - severity = "warning"
        receivers:
          - name: "blackhole"
          - name: "default"
            pushoverConfigs:
              - sendResolved: true
                html: true
                message: |-
                  {{- range .Alerts }}
                    {{- if ne .Annotations.description "" }}
                      {{ .Annotations.description }}
                    {{- else if ne .Annotations.summary "" }}
                      {{ .Annotations.summary }}
                    {{- else if ne .Annotations.message "" }}
                      {{ .Annotations.message }}
                    {{- else }}
                      Alert description not available
                    {{- end }}
                    {{- if gt (len .Labels.SortedPairs) 0 }}
                      <small>
                        {{- range .Labels.SortedPairs }}
                          <b>{{ .Name }}:</b> {{ .Value }}
                        {{- end }}
                      </small>
                    {{- end }}
                  {{- end }}
                priority: |-
                  {{ if eq .Status "firing" }}1{{ else }}0{{ end }}
                sound: gamelan
                title: >-
                  [{{ .Status | toUpper }}{{ if eq .Status "firing" }}:{{ .Alerts.Firing | len }}{{ end }}]
                  {{ .CommonLabels.alertname }}
                ttl: 86400s
                token:
                  name: *name
                  key: PUSHOVER_TOKEN
                userKey:
                  name: *name
                  key: PUSHOVER_USER_KEY
                urlTitle: View in Alertmanager
                # https_proxy is needed for stability
                # httpConfig:
                #   proxyURL: "${HTTPS_PROXY}"
                #   noProxy: "${NO_PROXY}"

    kubeApiServer:
      vmScrape:
        spec:
          endpoints:
            - bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
              port: https
              scheme: https
              tlsConfig:
                caFile: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
                serverName: kubernetes
              metricRelabelConfigs:
                # Drop high-cardinality histogram buckets (keep _sum/_count for averages).
                # Trade-off: K8s SLI dashboards lose P99 latency panels.
                - action: drop
                  sourceLabels: [__name__]
                  regex: (apiserver|etcd|rest_client)_request(|_sli|_slo)_duration_seconds_bucket
                - action: drop
                  sourceLabels: [__name__]
                  regex: (apiserver_response_sizes_bucket|apiserver_watch_events_sizes_bucket)
                # Admission control is patched out by Talos; no point in scraping.
                - action: drop
                  sourceLabels: [__name__]
                  regex: apiserver_admission_.+
                # Watch cache & long-running rq are platform-engineering metrics.
                - action: drop
                  sourceLabels: [__name__]
                  regex: apiserver_(longrunning_requests|registered_watchers|init_events_total)

    kubelet:
      vmScrape:
        spec:
          metricRelabelConfigs:
            # Drop high-cardinality labels.
            - action: labeldrop
              regex: (uid|id|name)
            - action: drop
              sourceLabels: [__name__]
              regex: (rest_client_request_duration_seconds_bucket|rest_client_request_duration_seconds_sum|rest_client_request_duration_seconds_count)
            # Optional, Drop cgroup metrics with no pod.
            - sourceLabels: [id, pod]
              action: drop
              regex: ".+;"
            # KEEP container_blkio_device_usage_total — needed for per-pod IOPS panels.
            # Optional, Drop less useful container blkio metrics.
            # - sourceLabels: [__name__]
            #   action: drop
            #   regex: "container_blkio_device_usage_total"
            # Optional, Drop less useful container filesystem io metrics.
            - sourceLabels: [__name__]
              action: drop
              regex: "container_fs_(io_current|io_time_seconds_total|io_time_weighted_seconds_total|reads_merged_total|sector_reads_total|sector_writes_total|writes_merged_total)"
            # Optional, Drop less useful container process metrics.
            - sourceLabels: [__name__]
              action: drop
              regex: "container_(tasks_state|threads_max)"
            # Drop kubelet runtime/PLEG/volume_manager bucket histograms — Talos changes
            # are infrequent, panels rarely consulted.
            - action: drop
              sourceLabels: [__name__]
              regex: (kubelet_runtime_operations|kubelet_pod_worker_duration_seconds|kubelet_pleg_relist_duration_seconds|volume_manager_.+)_bucket

    # disable chart components
    grafana:
      enabled: false
    prometheus-node-exporter:
      enabled: false
    kube-state-metrics:
      enabled: false

    # disable not-used metrics scraping
    kubeControllerManager:
      enabled: false
    kubeEtcd:
      enabled: false
    kubeProxy:
      enabled: false
    kubeScheduler:
      enabled: false

    # vmalertmanager rules
    additionalVictoriaMetricsMap:
      vlogs-rules:
        groups:
          - name: vlogs.rules
            type: vlogs
            interval: 5m
            rules:
              - alert: HasErrorLog
                expr: 'env: "prod" AND status:~"error|warn" | stats by (service, kubernetes.pod) count() as errorLog | filter errorLog:>0'
                annotations:
                  description: 'Service {{$labels.service}} (pod {{ index $labels "kubernetes.pod" }}) generated {{$labels.errorLog}} error logs in the last 5 minutes'
                labels:
                  severity: critical
          - name: ServiceRequest
            type: vlogs
            interval: 5m
            rules:
              - alert: TooManyFailedRequest
                expr: '* | extract "ip=<ip> " | extract "status_code=<code>;" | stats by (ip) count() if (code:~4.*) as failed, count() as total| math failed / total as failed_percentage| filter failed_percentage :> 0.01 | fields ip,failed_percentage'
                annotations:
                  description: "Connection from address {{$labels.ip}} has {{$value}}% failed requests in last 5 minutes"
                labels:
                  severity: critical
      dockerhub-rules:
        groups:
          - name: dockerhub.rules
            rules:
              - alert: DockerhubRateLimitRisk
                expr: count(time() - container_last_seen{image=~"(docker.io).*",container!=""} < 30) > 100
                annotations:
                  summary: Kubernetes cluster Dockerhub rate limit risk
                labels:
                  severity: critical
      oom-rules:
        groups:
          - name: oom.rules
            rules:
              - alert: OOMKilled
                expr: (kube_pod_container_status_restarts_total - kube_pod_container_status_restarts_total offset 10m >= 1) and ignoring (reason) min_over_time(kube_pod_container_status_last_terminated_reason{reason="OOMKilled"}[10m]) == 1
                annotations:
                  summary: Container {{ $labels.container }} in pod {{ $labels.namespace }}/{{ $labels.pod }} has been OOMKilled {{ $value }} times in the last 10 minutes.
                labels:
                  severity: critical
      # zfs-rules:
      #   groups:
      #     - name: zfs.rules
      #       rules:
      #         - alert: ZFSUnexpectedPoolState
      #           expr: node_zfs_zpool_state{state!="online"} > 0
      #           for: 15m
      #           annotations:
      #             summary: ZFS pool {{$labels.zpool}} on {{$labels.instance}} is in a unexpected state {{$labels.state}}
      #           labels:
      #             severity: critical
