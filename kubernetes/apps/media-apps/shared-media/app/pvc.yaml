---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
  namespace: media-apps
  # server path: /{shared-path}/media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  storageClassName: nfs-shared
