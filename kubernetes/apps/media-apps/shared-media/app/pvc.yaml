---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: shared-media
  namespace: media-apps
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  storageClassName: nfs-media
