---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
  namespace: media-apps
  # nfs-path: /var/lib/shared/media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  storageClassName: nfs-shared
