---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: storage
  namespace: media-apps
  # nfs-path: /var/lib/shared/storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: nfs-shared
