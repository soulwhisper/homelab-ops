---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: models
  namespace: media-apps
  # nfs-path: /var/lib/shared/models
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: nfs-shared
