---
apiVersion: v1
kind: Service
metadata:
  name: emqx-exporter
  labels:
    app: emqx-exporter
spec:
  selector:
    app: emqx-exporter
  ports:
    - name: metrics
      port: 8085
      targetPort: metrics
