---
endpoints:
  # internal
  - name: "${APP}"
    group: internal
    url: "https://${GATUS_SUBDOMAIN:=${APP}}.noirprime.com${GATUS_PATH:=/}"
    interval: 1m
    client:
      dns-resolver: "tcp://${DNS_HOST}:53"
    conditions:
      - "[STATUS] == ${GATUS_STATUS:=200}"
