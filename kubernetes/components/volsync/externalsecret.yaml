---
# yaml-language-server: $schema=https://kubernetes-schemas.noirprime.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: "${APP}-volsync"
spec:
  refreshInterval: 30m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      data:
        KOPIA_REPOSITORY: "s3://volsync/${APP}"
        KOPIA_PASSWORD: "{{ .volsync }}"
        AWS_S3_ENDPOINT: "http://rook-ceph-rgw-ceph-objectstore.storage-system.svc.cluster.local"
        AWS_ACCESS_KEY_ID: "{{ .volsync_access_key }}"
        AWS_SECRET_ACCESS_KEY: "{{ .volsync_secret_key }}"
  dataFrom:
    - extract:
        key: minio
    - extract:
        key: encryption_cipher
