---
# switch to a well-maintained watchtower repo

services:
  watchtower:
    image: "ghcr.m.daocloud.io/nicholas-fedor/watchtower:latest"
    container_name: "watchtower"
    restart: "unless-stopped"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
